<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:theme="@style/Theme.InstaChatViewer">
        <activity android:name=".ui.activity.ChatDetailActivity" />
        <activity android:name=".ui.activity.ChatListActivity" />
        <activity android:name=".ui.activity.UploadActivity" />
        <activity android:name=".ui.activity.DownloadGuideActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>



</manifest>